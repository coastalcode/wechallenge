import React, { Component } from 'react';
import { reduxForm } from 'redux-form';
import * as actions from '../../actions';

export default class Profile extends Component {
  render() {
    return (
      <div>your profile here</div>
      )
  }
}